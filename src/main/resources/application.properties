spring.application.name=PetPals
spring.datasource.url=jdbc:postgresql://localhost:5432/petpals
spring.datasource.username=${db_user}
spring.datasource.password=${db_password}
spring.jpa.hibernate.ddl-auto=update
# modificar el create drop a update para que no se borren los datos

# personalicen sus credenciales y puerto de postgres
# docker run --name petpals_db -e POSTGRES_PASSWORD=my_password -p 5434:5432 -d postgres
